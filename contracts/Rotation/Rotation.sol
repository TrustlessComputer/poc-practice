// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

import {IRotation} from "./IRotation.sol";

contract Rotation is IRotation {
    function solve(uint256[] calldata nums) external returns (uint256) {
        return 0;
    }
}
